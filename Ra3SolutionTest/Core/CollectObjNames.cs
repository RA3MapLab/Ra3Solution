using System.Collections.Generic;
using System.IO;
using System.Xml;
using MapCoreLib.Core.Util;

namespace NewMapParser.Core
{
    public class CollectObjNames
    {
        // public static string ra3MapNsUri = "uri:wu.com:ra3map";
        // public static XmlNamespaceManager ra3Ns = new XmlNamespaceManager(new NameTable());
        // public void Run()
        // {
        //     var res = new Dictionary<string, string>();
        //     ra3Ns.AddNamespace("ra3Ns", ra3MapNsUri);
        //     foreach (var file in Directory.EnumerateFiles(@"D:\file\yule\RA3_MODSDK-X_1.3\SageXml", "*.xml", SearchOption.AllDirectories))
        //     {
        //         XmlReaderSettings settings = new XmlReaderSettings();
        //         XmlReader reader = XmlReader.Create(file, settings);
        //         XmlDocument document = new XmlDocument();
        //         document.Load(reader);
        //         var objs = document.SelectNodes($"//ra3Ns:GameObject", ra3Ns);
        //         foreach (var obj in objs)
        //         {
        //             obj.
        //         }
        //     }
        // }
    }
}